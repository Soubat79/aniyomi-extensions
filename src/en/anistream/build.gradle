ext {
    extName = 'AniStream'
    extClass = '.AniStream'
    extVersionCode = 1
}
apply from: "$rootDir/common.gradle"
tasks.matching { it.name.startsWith("lintKotlin") }.configureEach { enabled = false }
